
  # Landing page with interactions (Copy)

  This is a code bundle for Landing page with interactions (Copy). The original project is available at https://www.figma.com/design/ilv1JS6VmxKarzWYIzItXD/Landing-page-with-interactions--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  